# 实验记录

## 2026-08-25 - Zotero 10 兼容与 0.0.4 发布

- 目的：增加 Zotero 10 支持，发布包含编译后 XPI 的 0.0.4 版本。
- 现象：Zotero 10 要求 `strict_max_version` 为 `10.0.*`；项目未调用 Zotero 10 删除的集合多选旧接口；原构建包会包含 `.DS_Store`。
- 判断：无需修改运行逻辑；需要同步插件清单与更新清单，并从构建包排除 `.DS_Store`。
- 修改：升级版本和兼容声明，增加 Zotero 10 启动配置与文档标识，清理 XPI 内容。
- 结果：构建和类型检查通过；XPI 内版本为 0.0.4，兼容上限为 10.0.*。
- 保留：是。
